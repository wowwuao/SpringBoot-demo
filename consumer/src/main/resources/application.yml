spring:
  application:
    name: consumer
  cloud:
    nacos:
      server-addr: http://127.0.0.1:8848
server:
  port: 5000
demo-service-provider:
  ribbon:
    NFLoadBalancerRuleClassName: com.alibaba.cloud.nacos.ribbon.NacosRule # nacos ??????
ribbon:
  eager-load:
    enabled: true # ??????
    clients:  # ?????????????????
      - demo-service-provider
feign:
  client:
    config:
      default: # default ?????
        logger-level: BASIC # ????,basic ???????????
  httpclient:
    enabled: true  # ?? feign ? httpClient ???
    max-connections: 200 # ??????
    max-connections-per-route: 50 # ??????????
